apiVersion: v1
kind: Service
metadata:
  name: backend-service
spec:
  selector:
    app: backend
  ports:
    - protocol: TCP
      port: 3000       # Service port
      targetPort: 3000 # Container port
  type: ClusterIP
